# config file version
apiVersion: 1

# List of alert rule UIDs that should be deleted
deleteContactPoints:
  # <int> organization ID, default = 1
  - orgId: 1
    # <string, required> unique identifier for the rule
    uid: <contactpoint_uid>